<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sk.brehy">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Brehy">
        <activity
            android:name=".LektoriLogin"
            android:exported="true" />
        <activity
            android:name=".Webstranka"
            android:exported="true" />
        <activity android:name=".AktualityContent" />
        <activity
            android:name=".Uvod"
            android:configChanges="orientation|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Lektori"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".Oznamy"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".Kontakt"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".Aktuality"
            android:configChanges="orientation|screenSize" />

        <service
            android:name=".GoogleMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>